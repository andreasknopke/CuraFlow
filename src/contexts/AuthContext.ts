/**
 * CuraFlow — AuthContext
 *
 * @dead — This file is empty and has no consumers.
 *         Auth is handled by src/components/AuthProvider.jsx instead.
 *         Candidate for deletion after TypeScript migration.
 */

export {};
